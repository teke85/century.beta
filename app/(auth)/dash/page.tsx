import type { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: "dash page",
    description: "this is a dash page",
};

const Dash = () => {
  return <section>Hello</section>;
};

export default Dash;
